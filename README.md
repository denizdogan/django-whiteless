# django-whiteless

Django template tags which deal with pesky whitespaces!

## Installation

```bash
$ pip install django-whiteless
```

```python
INSTALLED_APPS = (
    # ...
    "whiteless",
)
```
